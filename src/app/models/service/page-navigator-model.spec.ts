import {PageNavigatorModel} from './page-navigator-model';

describe('PageNavigatorModel', () => {
  it('should create an instance', () => {
    expect(new PageNavigatorModel()).toBeTruthy();
  });
});
