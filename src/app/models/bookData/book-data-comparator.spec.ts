import {BookDataComparator} from './book-data-comparator';

describe('BookDataComparator', () => {
  it('should create an instance', () => {
    expect(new BookDataComparator()).toBeTruthy();
  });
});
